# 2026-05-31 — 43服务器8005 RAG PDF报告能力改造

## 用户要求
ouev5指出应修改 43.99.62.219 的 8005 端口，并使用服务内已有 DeepSeek 配置，而不是修改本机其他 8005 服务。

## 纠错
- 先前误改了当前本机 `/root/.openclaw/workspace/online-file-converter`，不是目标服务器；已向用户说明并转向 43。
- 正确目标：43.99.62.219 `/www/wwwroot/literature_search/app.py`。

## 目标服务
- systemd：`literature-search.service`
- 端口：8005
- 进程入口：`/root/miniconda3/bin/python3 /www/wwwroot/literature_search/app.py`
- 原服务已有 DeepSeek：`deepseek-v4-pro`，用于 PubMed 检索词生成和文献分析。

## 改动
- 安装 `/root/miniconda3/bin/python3` 环境下的 `reportlab`。
- `app.py` 新增：
  - `ReportRequest`
  - `POST /api/report/submit`
  - `GET /api/report/download/{task_id}`
  - DeepSeek 生成/优化 PubMed 检索词
  - PubMed 检索并去重
  - DeepSeek 生成证据综合报告 JSON
  - reportlab 生成 PDF 报告
- 保留原有：`/api/search/submit`、`/api/deep-search/submit`、基因提取、CSV 下载等前端功能。
- 将 `uvicorn.run(... workers=2)` 改为 `workers=1`，修复内存态任务在多 worker 下轮询 404 的问题。

## 验证
- `python3 -m py_compile app.py`：通过
- `systemctl restart literature-search.service`：成功
- `GET http://127.0.0.1:8005/api/health`：返回 ok
- 测试报告任务：
  - 查询：`肢端肥大症的当前治疗策略`
  - task_id：`a8f881f2`
  - 状态流程：queued -> running -> done
  - JSON 结果：`/www/wwwroot/literature_search/results/a8f881f2.json`
  - PDF：`/www/wwwroot/literature_search/reports/rag_report_a8f881f2.pdf`
  - PDF：22KB，PDF 1.4，2 pages
  - SHA256：`5dc2981f12a3099b4e9674577296da1ab657bb6e85647b2245d9e75fc9cadd7c`
  - PubMed 文献数：12
  - DeepSeek 标题：`肢端肥大症的当前治疗策略：多模式综合管理`
  - 示例摘要含 PMID 引文：`[PMID:24915854]`, `[PMID:10970950]`, `[PMID:27522663]`

## 注意
- 43上没有 PyMuPDF/fitz，无法在43本地直接抽取PDF文本；但 `file` 已确认 PDF 类型和页数，结果 JSON 已确认 DeepSeek 内容和 PMID。

## 前端新增 Generate PDF Report 按钮
- 按 ouev5 要求，在 43.99.62.219:8005 前端搜索框旁新增 `Generate PDF Report` 按钮。
- 按钮调用 `doSearch(report)`，提交到 `POST /api/report/submit`。
- 报告生成完成后前端调用 `renderReportResults(data)`，展示：
  - PDF 下载按钮
  - DeepSeek PubMed 检索词
  - 执行摘要
  - 多方面报告 sections
  - Clinical / Research Progress
  - Future Directions
  - Limitations
  - PMID 可点击标签
  - Related Articles
- 保留原 `Search` 和 `Deep Search`。
- 前端公网检查：`curl http://43.99.62.219:8005/` 可检出 `Generate PDF Report`、`reportSection`、`renderReportResults`。
- 公网接口实测：查询 `肢端肥大症的当前治疗策略`，task_id `f106f62e`，完成状态 done。
- 新 PDF：`/www/wwwroot/literature_search/reports/rag_report_f106f62e.pdf`
- 报告标题：`肢端肥大症的当前治疗策略：循证综述`
- PubMed 文献数：8

## English-only UI and report text update
- ouev5 requested no Chinese in user-facing content.
- Converted front-end placeholder/popular examples/API validation messages/service name/report prompt/PDF section labels/fallback report text to English.
- DeepSeek report synthesis prompt now explicitly requires English report output.
- Public validation task: `f4ed6938`, query `acromegaly treatment strategy`, status done.
- Generated English title: `Acromegaly Treatment Strategy: An Evidence-Based Synthesis of Current and Emerging Approaches`.
- PDF: `/www/wwwroot/literature_search/reports/rag_report_f4ed6938.pdf`.
