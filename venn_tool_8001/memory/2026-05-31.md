
## 2026-05-31 Sankey-Dot Top N slider fix
- User wanted 8001 Gene Insights Hub Sankey-Bubble/Sankey-Dot plot to have a left-side filter control so dragging to Top 10 keeps only top 10 terms, Top 20 keeps top 20, etc.
- Project path: `/www/wwwroot/venn-tool`; service: `venn-tool.service` on port 8001.
- Investigation: backend `/api/sankey_bubble_plot` and `r_scripts/sankey_bubble.R` already supported `top_n` (1-100); `templates/index.html` already had a small numeric `Top` field inside the Sankey card top-right toolbar, but it was not visible as the left-side filter the user expected.
- Backup before changes: `templates/index.html.bak_20260531_021632_sankey_top_slider`, `app.py.bak_20260531_021632_sankey_top_slider`, `r_scripts/sankey_bubble.R.bak_20260531_021632_sankey_top_slider`.
- Change: added an orange left-sidebar control under the `Sankey-Dot Enrichment` button: `Sankey-Dot Top terms` range slider (`id=sankeyTopNSlider`, min 5 max 100 step 5) plus numeric input (`id=sankeyTopN`, min 1 max 100). Added JS helpers `clampSankeyTopN`, `updateSankeyTopNControls`, `syncSankeyTopNFromSlider`, `syncSankeyTopNFromInput`; existing `getSankeyOptions()` now syncs/reads the left-side control and passes `top_n` through to `/api/sankey_bubble_plot`.
- Removed duplicate small `Top` input from the Sankey card top-right toolbar to avoid confusion; sort control remains.
- Validation: `python3 -m py_compile app.py` passed; restarted `venn-tool.service`; `curl http://127.0.0.1:8001/` confirms new strings `Sankey-Dot Top terms`, `sankeyTopNSlider`, `syncSankeyTopNFromSlider`; test POSTs to `/api/sankey_bubble_plot` with `top_n=10` and `top_n=20` both returned HTTP 200 with image payloads.

## 2026-05-31 Sankey-Dot filter panel placement correction
- User clarified desired placement: not the global left control sidebar, but inside the Sankey-Bubble card itself, with a filter panel on the left and the chart on the right.
- Updated `/www/wwwroot/venn-tool/templates/index.html`: removed the previously added global sidebar Top terms control; wrapped the Sankey plot area in `#sankeyWorkspace` grid with `grid-template-columns:210px minmax(0,1fr)`. Added `#sankeyFilterPanel` inside `#sankeyCard` containing `Filter terms`, Top enriched terms slider (`#sankeyTopNSlider`), exact numeric input (`#sankeyTopN`), and Reset Top 25 button. Right side remains `#sankeyChartBox` / `#sankeyBubbleChart`.
- Backup before placement correction: `templates/index.html.bak_<timestamp>_sankey_card_left_panel`.
- Restarted `venn-tool.service`; `curl http://127.0.0.1:8001/` confirms `sankeyWorkspace`, `sankeyFilterPanel`, `Filter terms`, and `sankeyTopNSlider` are served.
